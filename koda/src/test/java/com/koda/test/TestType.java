/*******************************************************************************
* Copyright (c) 2013 Vladimir Rodionov. All Rights Reserved
*
* This code is released under the GNU Affero General Public License.
*
* See: http://www.fsf.org/licensing/licenses/agpl-3.0.html
*
* VLADIMIR RODIONOV MAKES NO REPRESENTATIONS OR WARRANTIES ABOUT THE SUITABILITY
* OF THE SOFTWARE, EITHER EXPRESS OR IMPLIED, INCLUDING BUT NOT LIMITED TO THE
* IMPLIED WARRANTIES OF MERCHANTABILITY, FITNESS FOR A PARTICULAR PURPOSE, OR
* NON-INFRINGEMENT. Vladimir Rodionov SHALL NOT BE LIABLE FOR ANY DAMAGES SUFFERED
* BY LICENSEE AS A RESULT OF USING, MODIFYING OR DISTRIBUTING THIS SOFTWARE OR
* ITS DERIVATIVES.
*
* Author: Vladimir Rodionov
*
*******************************************************************************/
package com.koda.test;



// TODO: Auto-generated Javadoc
/**
	 * The Enum Type.
	 */
	public enum TestType {

		/** The AD d_ index. */
		ADD_INDEX,
		/** The PUT. */
		PUT,

		/** The GET. */
		GET,

		/** The GE t_ bytes. */
		GET_BYTES,
		/** The GE t_ native. */
		GET_NATIVE,

		/** The GE t_ nativ e_ ba. */
		GET_NATIVE_BA,

		/** The GE t_ nativ e_ b a_2. */
		GET_NATIVE_BA_2,
		/** The REMOVE. */
		REMOVE,
		
		/** The UPDATE. */
		UPDATE,
		/** The MALLOC. */
		MALLOC,
		/** The PU t_ bytes. */
		PUT_BYTES,
		/** The GE t_ nativ e_ bb. */
		GET_NATIVE_BB,
		
		/** The GE t_ nativ e_ b b_ ptr. */
		GET_NATIVE_BB_PTR,
		
		/** The GE t_ nativ e_ b b_ pt r_ multi. */
		GET_NATIVE_BB_PTR_MULTI,
		
		/** The GE t_ nativ e_ b b_ pt r_ ptr. */
		GET_NATIVE_BB_PTR_PTR,
		/** The REMOV e2. */
		REMOVE_BYTES,

		/** The scan pointers. */
		RAW_SCAN,
		
		/** The KE y_ sca n_ buffer. */
		KEY_SCAN_BUFFER,
		
		/** The KE y_ sca n_ buffe r_ ptr. */
		KEY_SCAN_BUFFER_PTR,
		
		/** The KE y_ sca n_ bytes. */
		KEY_SCAN_BYTES,
		
		/** The KE y_ sca n_ byte s_ new. */
		KEY_SCAN_BYTES_NEW,
		
		/** The RECOR d_ sca n_ buffer. */
		RECORD_SCAN_BUFFER,
		
		/** The RECOR d_ sca n_ buffe r_ ptr. */
		RECORD_SCAN_BUFFER_PTR,
		
		/** The RECOR d_ sca n_ bytes. */
		RECORD_SCAN_BYTES,
		
		/** The RECOR d_ sca n_ byte s_ new. */
		RECORD_SCAN_BYTES_NEW,
		
		/** The VALU e_ sca n_ buffer. */
		VALUE_SCAN_BUFFER,
		
		/** The VALU e_ sca n_ buffe r_ ptr. */
		VALUE_SCAN_BUFFER_PTR,
		
		/** The VALU e_ sca n_ bytes. */
		VALUE_SCAN_BYTES,
		
		/** The VALU e_ sca n_ byte s_ new. */
		VALUE_SCAN_BYTES_NEW,
		
		/** The PERFORMANCE. */
		PERFORMANCE, 
		
		/** The CHEC k_ inde x_ size. */
		CHECK_INDEX_SIZE, 
		
		/** The INDE x_ scan. */
		INDEX_SCAN, 
		
		/** The INDE x_ sca n_ bulk. */
		INDEX_SCAN_BULK, 
		
		/** The INDE x_ sca n_ bul k_ real. */
		INDEX_SCAN_BULK_REAL, 
		
		/** The INDE x_ sca n_ bul k_ shared. */
		INDEX_SCAN_BULK_SHARED, 
		
		
		/** The AD d_ inde x_ skewed. */
		ADD_INDEX_SKEWED, 
		
		/** The INDE x_ sca n_ bul k_ multi. */
		INDEX_SCAN_BULK_MULTI, 
		
		/** The PU t_ bytebuffer. */
		PUT_BYTEBUFFER,
		
		/** The GE t_ nativ e_ b b_ key. */
		GET_NATIVE_BB_KEY
	}
